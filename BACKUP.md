# 数据备份与恢复

登录系统后，从首页进入“数据备份”。

## 下载当前备份

“下载当前备份”会通过 SQLite 在线备份 API 生成一致的 `.db` 文件，即使系统使用 WAL 模式也不会遗漏最新数据。

建议定期把下载文件保存到另一台设备或云盘。项目内的自动备份仍与电脑处在同一块磁盘上，不能替代异地副本。

## 自动备份

- 每天第一次启动服务时自动备份一次。
- 日常备份最多保留 30 份。
- 文件保存在项目的 `data_backups/` 目录。
- 备份目录与文件仅允许当前 macOS 用户访问。

## 恢复

上传 `.db` 文件后，系统会先检查：

- SQLite 完整性；
- `locations` 和 `items` 表及必需字段；
- 物品与位置之间的外键引用。

检查通过后，系统会先创建 `pre_restore` 安全副本，再替换当前数据。恢复前副本最多保留 20 份。

恢复只影响物品和位置数据库，不会修改登录用户名、密码哈希或会话密钥。
